version: '3.9'
services:
  jp-crm-buddy:
    image: postgres
    restart: always
    container_name: jp-crm-buddy
    ports:
      - '5433:5432'
    environment:
      POSTGRES_PASSWORD: example
      PGDATA: /data/postgres
    volumes:
      - postgres:/data/postgres

volumes:
  postgres:
